#!/bin/bash
# bin/awx-deploy - Script principal de implantação AWX modularizado

set -euo pipefail

# Configuração de diretórios
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"
LIB_DIR="$PROJECT_ROOT/lib"
CONFIG_DIR="$PROJECT_ROOT/config"

# Carregar módulos core
source "$LIB_DIR/core/logger.sh"
source "$LIB_DIR/core/validator.sh"
source "$LIB_DIR/core/resource_calculator.sh"
source "$LIB_DIR/utils/common.sh"

# Carregar serviços
source "$LIB_DIR/services/dependency_manager.sh"
source "$LIB_DIR/services/kind_manager.sh"
source "$LIB_DIR/services/registry_manager.sh"
source "$LIB_DIR/services/ee_builder.sh"
source "$LIB_DIR/services/awx_installer.sh"

# Configurações padrão
DEFAULT_HOST_PORT=8080
DEFAULT_REGISTRY_PORT=5001
DEFAULT_AWX_NAMESPACE="awx"

# Variáveis globais
CLUSTER_NAME=""
HOST_PORT=""
FORCE_CPU=""
FORCE_MEM_MB=""
INSTALL_DEPS_ONLY=false
VERBOSE=false
CONFIG_FILE=""

# Função de ajuda
show_help() {
    cat << EOF
$(log_header "Script de Implantação AWX com Kind - Versão Modular")

$(echo -e "${LOG_COLORS[WHITE]}USO:${LOG_COLORS[NC]}")
    $0 [OPÇÕES]...

$(echo -e "${LOG_COLORS[WHITE]}OPÇÕES:${LOG_COLORS[NC]}")
    $(echo -e "${LOG_COLORS[GREEN]}-c NOME${LOG_COLORS[NC]}")      Nome do cluster Kind (padrão: será calculado baseado no perfil)
    $(echo -e "${LOG_COLORS[GREEN]}-p PORTA${LOG_COLORS[NC]}")     Porta do host para acessar AWX (padrão: 8080)
    $(echo -e "${LOG_COLORS[GREEN]}-f CPU${LOG_COLORS[NC]}")       Forçar número de CPUs (ex: 4)
    $(echo -e "${LOG_COLORS[GREEN]}-m MEMORIA${LOG_COLORS[NC]}")   Forçar quantidade de memória em MB (ex: 8192)
    $(echo -e "${LOG_COLORS[GREEN]}-d${LOG_COLORS[NC]}")           Instalar apenas dependências
    $(echo -e "${LOG_COLORS[GREEN]}-v${LOG_COLORS[NC]}")           Modo verboso (debug)
    $(echo -e "${LOG_COLORS[GREEN]}-C CONFIG${LOG_COLORS[NC]}")    Arquivo de configuração personalizado
    $(echo -e "${LOG_COLORS[GREEN]}-h${LOG_COLORS[NC]}")           Exibir esta ajuda

$(echo -e "${LOG_COLORS[WHITE]}EXEMPLOS:${LOG_COLORS[NC]}")
    $0                                    # Usar valores padrão
    $0 -c meu-cluster -p 8080            # Cluster personalizado na porta 8080
    $0 -f 4 -m 8192                     # Forçar 4 CPUs e 8GB RAM
    $0 -d                                # Instalar apenas dependências
    $0 -v -c test-cluster                # Modo verboso com cluster personalizado
    $0 -C config/custom.conf             # Usar configuração personalizada

$(echo -e "${LOG_COLORS[WHITE]}CONFIGURAÇÃO:${LOG_COLORS[NC]}")
    As configurações podem ser personalizadas através de:
    - Variáveis de ambiente
    - Arquivos de configuração em $CONFIG_DIR
    - Parâmetros de linha de comando (maior prioridade)

$(echo -e "${LOG_COLORS[WHITE]}LOGS:${LOG_COLORS[NC]}")
    Configure LOG_LEVEL (DEBUG|INFO|WARNING|ERROR) para controlar verbosidade
    Configure LOG_FILE para salvar logs em arquivo

EOF
}

# Processamento de argumentos
process_arguments() {
    while getopts "c:p:f:m:dvC:h" opt; do
        case ${opt} in
            c)
                CLUSTER_NAME="$OPTARG"
                ;;
            p)
                HOST_PORT="$OPTARG"
                ;;
            f)
                FORCE_CPU="$OPTARG"
                ;;
            m)
                FORCE_MEM_MB="$OPTARG"
                ;;
            d)
                INSTALL_DEPS_ONLY=true
                ;;
            v)
                VERBOSE=true
                LOG_LEVEL="DEBUG"
                ;;
            C)
                CONFIG_FILE="$OPTARG"
                ;;
            h)
                show_help
                exit 0
                ;;
            *)
                log_error "Opção inválida: -$OPTARG"
                show_help
                exit 1
                ;;
        esac
    done
    shift $((OPTIND - 1))
}

# Carregamento de configurações
load_configuration() {
    # Carregar configuração padrão
    local default_config="$CONFIG_DIR/default.conf"
    if [[ -f "$default_config" ]]; then
        log_debug "Carregando configuração padrão: $default_config"
        source "$default_config"
    fi
    
    # Carregar configuração personalizada se especificada
    if [[ -n "$CONFIG_FILE" ]]; then
        if [[ -f "$CONFIG_FILE" ]]; then
            log_debug "Carregando configuração personalizada: $CONFIG_FILE"
            source "$CONFIG_FILE"
        else
            log_error "Arquivo de configuração não encontrado: $CONFIG_FILE"
            exit 1
        fi
    fi
    
    # Configurar logging
    configure_logging
}

# Inicialização do sistema
initialize_system() {
    log_header "INICIALIZAÇÃO DO SISTEMA"
    
    # Validar parâmetros de entrada
    if ! validate_input_parameters "$CLUSTER_NAME" "$HOST_PORT" "$FORCE_CPU" "$FORCE_MEM_MB"; then
        exit 1
    fi
    
    # Inicializar cálculos de recursos
    initialize_resource_calculations
    
    # Definir valores padrão baseados nos cálculos
    CLUSTER_NAME=${CLUSTER_NAME:-"awx-cluster-${SYSTEM_RESOURCES[PROFILE]}"}
    HOST_PORT=${HOST_PORT:-$DEFAULT_HOST_PORT}
    
    # Exportar variáveis para compatibilidade com módulos legacy
    export_resource_variables
    export CLUSTER_NAME HOST_PORT AWX_NAMESPACE
    export REGISTRY_PORT=$DEFAULT_REGISTRY_PORT
    export AWX_NAMESPACE=$DEFAULT_AWX_NAMESPACE
    
    # Validar disponibilidade da porta
    if ! check_port_availability "$HOST_PORT" "AWX"; then
        exit 1
    fi
    
    log_success "Sistema inicializado com sucesso"
}

# Função principal de implantação
deploy_awx() {
    log_header "INICIANDO IMPLANTAÇÃO AWX"
    
    log_info "💻 Configuração do Sistema:"
    log_info "   CPUs: ${SYSTEM_RESOURCES[CPU_CORES]}"
    log_info "   Memória: ${SYSTEM_RESOURCES[MEMORY_MB]}MB"
    log_info "   Perfil: ${SYSTEM_RESOURCES[PROFILE]}"
    
    log_info "🎯 Configuração de Implantação:"
    log_info "   Cluster: $CLUSTER_NAME"
    log_info "   Porta: $HOST_PORT"
    log_info "   Namespace: $AWX_NAMESPACE"
    log_info "   Web Réplicas: ${AWX_RESOURCES[WEB_REPLICAS]}"
    log_info "   Task Réplicas: ${AWX_RESOURCES[TASK_REPLICAS]}"
    
    # Executar etapas de implantação
    install_all_dependencies
    
    if [[ "$INSTALL_DEPS_ONLY" == "true" ]]; then
        log_success "✅ Dependências instaladas com sucesso!"
        log_info "Execute o script novamente sem a opção -d para instalar o AWX"
        exit 0
    fi
    
    create_kind_cluster
    create_execution_environment
    install_awx_complete
    wait_for_awx_ready
    get_awx_credentials
    show_deployment_summary
    
    log_success "🎉 Implantação do AWX concluída com sucesso!"
}

# Função principal
main() {
    # Configurar tratamento de erros
    trap 'log_error "Erro na linha $LINENO. Comando: $BASH_COMMAND"' ERR
    
    # Processar argumentos e configurações
    process_arguments "$@"
    load_configuration
    
    # Validar pré-requisitos se não for apenas instalação de dependências
    if [[ "$INSTALL_DEPS_ONLY" == "false" ]]; then
        if ! validate_prerequisites; then
            exit 1
        fi
    fi
    
    # Inicializar sistema
    initialize_system
    
    # Executar implantação
    deploy_awx
}

# Executar função principal se script for chamado diretamente
if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    main "$@"
fi
